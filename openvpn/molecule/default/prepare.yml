- name: Prepare
  hosts: all
  gahter_facts: false
  tasks:
    - name: Install Python for Ansible
      raw: test -e /usr/bin/python || (apt -y update && apt install -y python)
      become: true
      changed_when: false